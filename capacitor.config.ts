import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'playacaribe-rental-car-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
